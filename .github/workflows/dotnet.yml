name: dotnet-self-hosted

on:
  push:
    branches:
      - "m*-validation"
      - "p*-validation"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-test:
    runs-on: [self-hosted, yowthi-erp-v2]
    timeout-minutes: 30

    defaults:
      run:
        shell: cmd

    env:
      DOTNET_CLI_TELEMETRY_OPTOUT: "1"
      DOTNET_NOLOGO: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Verify .NET SDK
        run: |
          dotnet --version
          for /f "delims=" %%V in ('dotnet --version') do if not "%%V"=="10.0.400" exit /b 1

      - name: Verify Node.js and pnpm
        run: |
          node --version
          pnpm --version
          for /f "delims=" %%V in ('pnpm --version') do if not "%%V"=="11.23.0" exit /b 1

      - name: Restore local tools
        run: dotnet tool restore

      - name: Restore
        run: dotnet restore YowThi.Erp.slnx

      - name: Build
        run: dotnet build YowThi.Erp.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test YowThi.Erp.slnx --configuration Release --no-build

      - name: Frontend install
        working-directory: src/YowThi.Erp.Web
        run: pnpm install --frozen-lockfile

      - name: Frontend lint
        working-directory: src/YowThi.Erp.Web
        run: pnpm lint

      - name: Frontend typecheck
        working-directory: src/YowThi.Erp.Web
        run: pnpm typecheck

      - name: Frontend build
        working-directory: src/YowThi.Erp.Web
        run: pnpm build
